import React from "react";

import { Container } from "./styles";

const Header: React.FC = () => {
  return <Container>Interface Reproduzida em ReactJS + TypeScript para propositos de estudo. </Container>;
};

export default Header;
